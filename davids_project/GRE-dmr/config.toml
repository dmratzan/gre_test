baseURL = "/"
languageCode = "en-us"
title = "Gardens of the Roman Empire (DMR)"
theme = "mainroad"

[Params]
  subtitle = "An ISAW prototype" # Deprecated in favor of .Site.Params.logo.subtitle
  description = "DMR's first pass at a GRE hugo website" # Site description. Used in meta description
  copyright = "GRE project team" # Footer copyright holder, otherwise will use site title
  twitter_cards = true # Enable Twitter Cards if true
  authorbox = false # Show authorbox at bottom of pages if true
  toc = true # Enable Table of Contents
  pager = true # Show pager navigation (prev/next links) at the bottom of pages if true
  post_meta = ["author", "date", "categories", "translations"] # Order of post meta information
  mainSections = ["provinces", "blog", "news"] # Specify section pages to show on home page and the "Recent articles" widget
  dateformat = "2006-01-02" # Change the format of dates
  highlightColor = "#e22d30" # Override highlight color

[Params.logo]
  image = "img/placeholder.png" # Logo image. Path relative to "static"
  title = "Gardens of the Roman Empire (DMR)" # Logo title, otherwise will use site title
  subtitle = "An ISAW prototype" # Logo subtitle

[Params.sidebar]
  home = "right" # Configure layout for home page
  list = "left"  # Configure layout for list pages
  single = false # Configure layout for single pages
  # Enable widgets in given order
  widgets = ["search", "recent", "categories", "taglist", "social", "languages"]

[Params.widgets]
  recent_num = 5 # Set the number of articles in the "Recent articles" widget
  tags_counter = false # Enable counter for each tag in "Tags" widget

[Params.widgets.social]
  # Enable parts of social widget
  facebook = "username"
  twitter = "username"
  instagram = "username"
  linkedin = "username"
  telegram = "username"
  github = "username"
  gitlab = "username"
  bitbucket = "username"
  email = "example@example.com"
